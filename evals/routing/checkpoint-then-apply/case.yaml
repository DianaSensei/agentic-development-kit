schema_version: "1.1"
name: checkpoint-then-apply
context:
  scaffold_script: fixture.sh
  # A recorded bug-fix session on the same fixture that stopped at its Checkpoint,
  # proposing a fix and asking to proceed. Kept to the conversation only: no
  # environment, session context or prompt snapshots.
  history_file: history.jsonl

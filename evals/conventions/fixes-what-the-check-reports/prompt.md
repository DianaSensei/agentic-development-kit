I've already decided on this one, so edit the file directly - no workflow, no plan: add a function
`apply_discount(total, percent)` to src/app/pricing.py that returns the total after the discount and
logs the discount it applied.

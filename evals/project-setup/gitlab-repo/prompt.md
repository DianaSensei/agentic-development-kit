---
description: A repo on a self-managed GitLab gets the GitLab reviewer - one include added to the existing .gitlab-ci.yml, keeping its jobs and naming one of its own stages - and GitLab's MCP server in .mcp.json, with nothing written for GitHub.
tags: [project-setup, code-host]
max_turns: 40
timeout_seconds: 900
allowed_tools: [Read, Glob, Grep, Skill]
---

Set this repo up for the agentic development kit. Don't ask me anything - apply the defaults and skip whatever needs my input.
